<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.journalista.mappers.memberMapper">

	<!-- 회원 가입 -->
	<insert id="register"
		parameterType="kr.co.journalista.member.MemberVO">
		insert into myMember(userId, userPass, userName)
		values(#{userId}, #{userPass}, #{userName})
	</insert>

	<!-- 로그인 -->
	<select id="login" resultType="kr.co.journalista.member.MemberVO">
		select
		userId, userName
		from
		myMember
		where userId = #{userId}
		and userPass = #{userPass}
	</select>

	<!-- 아이디 확인 -->
	<select id="idCheck" resultType="kr.co.journalista.member.MemberVO">
		select userId from myMember
		    where userId = #{userId}
	</select>

</mapper>